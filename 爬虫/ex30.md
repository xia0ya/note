# 练习三十：exp4扩展，绕过翻页限

 exp4扩展，绕过翻页限制 pagesize参数使用

---

## 模板

```python
# encoding: utf-8
# @Author: xia0ya

import requests
from lxml import etree
import re

url = "https://spiderbuf.cn/web-scraping-practice/web-pagination-scraper?pageno=1&pagesize=50"
MyHeaders = {'user-agent':"Mozilla/5.0 (Linux; Android 6.0; Nexus 5 Build/MRA58N) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/137.0.0.0 Mobile Safari/537.36 Edg/137.0.0.0"}

html = requests.get(url, headers=MyHeaders).text
# print(html)   # 获取网页源码

# 保存文件
# f = open("exp02.html", "w", encoding="utf-8")
# f.write(html)
# f.close()

# 解析数据 xpath语法需要注意
root = etree.HTML(html)
trs = root.xpath('//tr')

# f = open("data01.txt", "w", encoding='utf-8')

for tr in trs:
    tds = tr.xpath('./td')
    s = ''
    for td in tds:
        s = s + str(td.xpath('string(.)')) + '|'
        # s = s + str(td.text) + '|'
    print(s)
#     if s != 0:
#         f.write(s + '\n')
# f.close()
```
---

## 说明
